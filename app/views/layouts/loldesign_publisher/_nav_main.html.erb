<nav class='main'>
  <figure>
    <%= image_tag LoldesignPublisher.config[:logo] %>
  </figure>
  <ul>
      <% LoldesignPublisher.config[:menu_links].each_key do |key|%>
        <li>
          <%= link_to LoldesignPublisher.config[:menu_links][key]['name'], LoldesignPublisher.config[:menu_links][key]['path'] %>
        </li>
      <% end -%>
    </ul>
</nav>